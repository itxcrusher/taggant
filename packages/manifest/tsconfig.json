{
  "extends": "../../tsconfig.base.json",
  // tsc only typechecks here; tsup does the build, so there is nothing to root.
  "compilerOptions": { "noEmit": true },
  "include": ["src", "test", "schema"]
}
